# Invocation golden files

Generated by the deterministic test rand source (seqReader in
helpers_test.go): session id = base64url(bytes 0x00..0x0F). Synthetic —
no real endpoints (ADR-0007). Shape per OpenID4VP 1.0 §5: by-reference
authorization request carrying client_id, request_uri,
request_uri_method=get only.
